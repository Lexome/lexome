import { styled } from "@/theme";

export const CenterInSpace = styled('div', {
  styles: {
    display: 'flex',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
    alignItems: 'center',
  }
})